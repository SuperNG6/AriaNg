# AriaNg 2.1.2

本次补丁版修复 All-In-One 构建在现代浏览器中的字体加载问题。

## 修复

- 移除已废弃的 `minimal-ui` viewport 参数，避免现代浏览器输出无效参数警告。
- All-In-One 构建现在内联 Font Awesome 的 WOFF2 字体，不再请求不存在的 `fonts/fontawesome-webfont.woff2`，图标可在单文件部署中正常显示。
