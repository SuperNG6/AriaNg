# BT Filter Runtime Audit Plan

Date: 2026-07-14

1. Reproduce the missing Downloading filter rail in a real Playwright browser connected to the supplied aria2 endpoint and capture console evidence.
2. Audit every Angular interpolation before and after the same HTML minification used by Gulp; remove ambiguous inline object literals from interpolation boundaries.
3. Verify controller scope bindings, dependency injection, route conditions, translations, script order, and generated bundle contents.
4. Fix the development template-cache pipeline so `gulp serve` cannot serve stale views after a source change.
5. Audit the durable sequential executor for eligibility changes, restart recovery, rollback failure, RPC switching, and queue progress.
6. Add behavior regressions for every confirmed defect, then run the complete test, lint, standard-build, bundle-build, and runtime gates.
