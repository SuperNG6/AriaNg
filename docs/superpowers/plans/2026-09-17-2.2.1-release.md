# AriaNg 2.2.1 patch release

Scope: bulk-filter pause ownership fix and behavior-preserving cleanup. No new
features, dependencies, translations, or RPC protocols.

Synchronize package/lock versions, release workflow default, contract tests, and
release notes. Exclude pre-existing unrelated worktree changes from the commit.

Acceptance: all tests, all-language i18n contract, both clean builds and archive
integrity/content checks. Verify remote version/tag availability before publishing
and monitor the release workflow through completion. The specific paused/ineligible
transition is mock-verified; browser checks cover real-backend connectivity and preview.

Local acceptance passed: `npm test`, `npm run test:i18n-release`, standard and
All-In-One clean builds (including lint), ZIP integrity/required entries/version
checks, embedded bundle fonts, and `git diff --check`. Builds ran in a disposable
source copy to preserve the active source preview; GitHub rebuilds the tagged commit.
